apiVersion: v1
kind: Service
metadata:
  name: python-api

spec:
  type: {{ .Values.service.type }}

  selector:
    app: python-api

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}